**Unreleased**

* Changed the API key asset field to a password field. Existing assets should rotate and re-enter the API key after upgrading.
* Enabled TLS certificate verification by default. Existing assets must trust the API server certificate chain or explicitly disable verification.
